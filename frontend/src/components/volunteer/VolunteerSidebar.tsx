import { NavLink } from 'react-router-dom';
import { Activity, AlertTriangle, Award, Bell, BookOpen, ClipboardList, FileCheck, Heart, LayoutDashboard, Map, Settings, ShieldCheck, User } from 'lucide-react';
const links = [
  ['Dashboard','/volunteer/dashboard', LayoutDashboard], ['Nearby Emergencies','/volunteer/emergencies', AlertTriangle], ['Emergency Map','/volunteer/map', Map], ['My Assignments','/volunteer/assignments', ClipboardList], ['Care Credits','/volunteer/care-credits', Heart], ['Verification','/volunteer/verification', ShieldCheck], ['Certificates','/volunteer/certificates', Award], ['Training','/volunteer/training', BookOpen], ['My Impact','/volunteer/impact', Activity], ['History','/volunteer/history', FileCheck], ['Notifications','/volunteer/notifications', Bell], ['Profile','/volunteer/profile', User], ['Privacy','/volunteer/privacy', ShieldCheck], ['Settings','/volunteer/settings', Settings],
] as const;
export default function VolunteerSidebar() { return <aside className="w-64 bg-slate-900 border-r border-slate-800 shrink-0 hidden md:flex flex-col"><div className="px-6 py-6 flex items-center gap-3"><span className="p-2.5 rounded-xl bg-indigo-600"><Heart className="w-5 h-5" /></span><div><h1 className="font-black text-white">AegisNet</h1><p className="text-[9px] uppercase tracking-widest text-slate-500">Volunteer Network</p></div></div><nav className="px-3 space-y-1 overflow-y-auto">{links.map(([label,to,Icon]) => <NavLink key={to} to={to} className={({isActive}) => `flex gap-3 items-center rounded-xl px-3.5 py-2.5 text-xs font-semibold ${isActive ? 'bg-indigo-600 text-white' : 'text-slate-400 hover:bg-slate-800 hover:text-white'}`}><Icon className="w-4 h-4"/>{label}</NavLink>)}</nav><p className="mt-auto p-4 text-center text-[10px] text-slate-600">Verified Community Response</p></aside>; }
