import { useEffect, useState } from 'react';
import { Link, Outlet, useLocation, useNavigate } from 'react-router-dom';
import { Bell, LogOut, Menu, ShieldCheck, LayoutDashboard, AlertTriangle, Map, ClipboardList } from 'lucide-react';
import NotificationBell from '../notifications/NotificationBell';
import VolunteerSidebar from './VolunteerSidebar';
export default function VolunteerLayout() { const nav=useNavigate(), loc=useLocation(), [session,setSession]=useState<any>(); useEffect(()=>{try { const s=JSON.parse(localStorage.getItem('aegis_session') || 'null'); if (!s?.authenticated || s.role !== 'volunteer') { nav('/volunteer/login'); return; } setSession(s); } catch { nav('/volunteer/login'); }},[nav]); if(!session) return <div className="min-h-screen bg-slate-950 grid place-items-center text-slate-400">Authorizing responder credentials…</div>; const mobile=[['Dashboard','/volunteer/dashboard',LayoutDashboard],['Emergencies','/volunteer/emergencies',AlertTriangle],['Map','/volunteer/map',Map],['Missions','/volunteer/assignments',ClipboardList]] as const; return <div className="min-h-screen flex bg-slate-950 text-slate-100 font-sans"><VolunteerSidebar/><div className="flex-1 min-w-0 pb-16 md:pb-0"><header className="h-16 px-5 bg-slate-900 border-b border-slate-800 flex items-center justify-between"><div className="flex gap-2 items-center"><Menu className="md:hidden w-5 h-5 text-slate-400"/><div><h2 className="text-sm font-black">Community Response Console</h2><p className="text-[9px] text-slate-500">Verified assistance only</p></div></div><div className="flex gap-3 items-center"><NotificationBell role="volunteer"/><span className="hidden sm:flex gap-1.5 text-[10px] text-emerald-400"><ShieldCheck className="w-3.5 h-3.5"/>Verified</span><button onClick={()=>{localStorage.removeItem('aegis_session');nav('/volunteer/login')}} className="text-xs text-slate-400 hover:text-white flex gap-1"><LogOut className="w-4 h-4"/>Logout</button></div></header><main><Outlet/></main></div><nav className="md:hidden fixed bottom-0 inset-x-0 z-40 bg-slate-900 border-t border-slate-800 flex justify-around p-2">{mobile.map(([label,to,Icon])=><Link key={to} to={to} className={`text-[9px] flex flex-col items-center ${loc.pathname===to?'text-indigo-400':'text-slate-400'}`}><Icon className="w-5 h-5"/>{label}</Link>)}</nav></div>; }
