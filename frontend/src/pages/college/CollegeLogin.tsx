import { useState } from 'react';import { useNavigate } from 'react-router-dom';import { Building2,Lock } from 'lucide-react';
export default function CollegeLogin(){const[email,setEmail]=useState('admin@examplemedical.edu'),[password,setPassword]=useState('college123'),[error,setError]=useState(''),nav=useNavigate();const submit=async(e:React.FormEvent)=>{e.preventDefault();const r=await fetch('/api/college/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});if(!r.ok)return setError((await r.json()).detail);localStorage.setItem('aegis_session',JSON.stringify((await r.json()).session));nav('/college/dashboard')};return <div className="min-h-screen grid place-items-center bg-slate-950 p-6"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-slate-900 border border-slate-800 p-8 space-y-5"><div className="text-center"><span className="inline-flex p-3 bg-indigo-600 rounded-2xl"><Building2/></span><h1 className="font-black text-lg mt-3">College Admin Portal</h1><p className="text-xs text-slate-400 mt-1">Secure institutional access</p></div>{error&&<p className="text-xs text-rose-400">{error}</p>}<label className="block text-xs text-slate-400">Institution email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-1 p-3 w-full rounded-xl bg-slate-950 border border-slate-800 text-white"/></label><label className="block text-xs text-slate-400">Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-1 p-3 w-full rounded-xl bg-slate-950 border border-slate-800 text-white"/></label><button className="w-full bg-indigo-600 py-3 rounded-xl font-bold text-sm">Sign In</button><p className="pt-4 border-t border-slate-800 flex gap-2 text-[10px] text-slate-500"><Lock className="w-4 h-4"/>Access is limited to volunteer verification and anonymized community-care administration.</p></form></div>}
