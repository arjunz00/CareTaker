import { useEffect,useState } from 'react';import { useParams } from 'react-router-dom';
export default function StudentDetail(){const{id}=useParams(),[student,setStudent]=useState<any>();useEffect(()=>{fetch(`/api/college/students/${id}`).then(r=>r.json()).then(setStudent)},[id]);if(!student)return <div className="p-6 text-slate-400">Loading student profile…</div>;return <div className="p-6 max-w-3xl"><h1 className="text-xl font-black">{student.name}</h1><p className="text-xs text-slate-500 mt-1">Student volunteer profile · no patient health data</p><div className="mt-6 p-5 bg-slate-900 border border-slate-800 rounded-3xl"><pre className="whitespace-pre-wrap text-xs leading-6 text-slate-300">{JSON.stringify(student,null,2)}</pre></div></div>}
