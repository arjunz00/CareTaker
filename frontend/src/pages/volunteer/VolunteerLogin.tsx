import { useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { Heart, Lock, ShieldCheck } from 'lucide-react';
export default function VolunteerLogin(){const [email,setEmail]=useState('rahul.volunteer@aegisnet.demo'),[password,setPassword]=useState('volunteer123'),[error,setError]=useState(''),[loading,setLoading]=useState(false),nav=useNavigate(); const submit=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);setError('');const r=await fetch('/api/volunteer/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});setLoading(false);if(!r.ok)return setError((await r.json()).detail);const d=await r.json();localStorage.setItem('aegis_session',JSON.stringify(d.session));nav('/volunteer/dashboard')};return <div className="min-h-screen bg-slate-950 grid place-items-center p-6"><form onSubmit={submit} className="w-full max-w-md p-8 rounded-3xl bg-slate-900 border border-slate-800 shadow-2xl space-y-5"><div className="text-center"><span className="inline-flex p-3 rounded-2xl bg-indigo-600"><Heart/></span><h1 className="mt-3 text-lg font-black">Volunteer Portal</h1><p className="text-xs text-slate-400 mt-1">Secure verified community-care access</p></div>{error&&<p className="p-3 rounded-xl text-xs text-rose-300 bg-rose-950/30 border border-rose-900">{error}</p>}<label className="block text-xs text-slate-400">Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" className="mt-1 w-full p-3 rounded-xl bg-slate-950 border border-slate-800 text-white"/></label><label className="block text-xs text-slate-400">Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" className="mt-1 w-full p-3 rounded-xl bg-slate-950 border border-slate-800 text-white"/></label><button disabled={loading} className="w-full rounded-xl bg-indigo-600 hover:bg-indigo-500 py-3 font-bold text-sm">{loading?'Signing in…':'Sign In'}</button><div className="text-center text-[10px] text-slate-500"><Link className="text-indigo-400" to="/volunteer/register">Apply to volunteer</Link> · Demo credentials are prefilled</div><p className="border-t border-slate-800 pt-4 flex gap-2 text-[10px] text-slate-500"><Lock className="w-4 h-4 shrink-0"/>Patient information is restricted to what is required during an assigned emergency.</p></form></div>}
