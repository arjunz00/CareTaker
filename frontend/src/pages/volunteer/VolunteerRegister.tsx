import { useState } from 'react'; import { useNavigate } from 'react-router-dom';
const steps=['Personal','Academic','Skills','Training','Documents','Consent'];
export default function VolunteerRegister(){const [step,setStep]=useState(0),[sent,setSent]=useState(false),nav=useNavigate(); const submit=async()=>{await fetch('/api/volunteer/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:'New Volunteer',email:'new@example.com',phone:'+91 90000 00000',college:'Example Medical College',course:'B.Pharm',skills:['Basic First Aid']})});setSent(true)}; if(sent)return <div className="min-h-screen bg-slate-950 grid place-items-center p-6"><div className="max-w-md p-8 bg-slate-900 border border-slate-800 rounded-3xl text-center"><h1 className="font-black text-xl">Application submitted</h1><p className="text-sm text-slate-400 mt-3">Your college and training records are pending review. We will not claim government verification without an integrated service.</p><button onClick={()=>nav('/volunteer/login')} className="mt-5 bg-indigo-600 px-5 py-3 rounded-xl">Go to sign in</button></div></div>;return <div className="min-h-screen bg-slate-950 p-6 text-slate-100"><div className="max-w-2xl mx-auto bg-slate-900 border border-slate-800 rounded-3xl p-6"><p className="text-xs text-indigo-400">Step {step+1} of 6 · {steps[step]}</p><h1 className="text-xl font-black mt-1">Volunteer application</h1><p className="text-sm text-slate-400 mt-2">Verification protects patients, volunteers, and the community-care network.</p><div className="mt-6 p-5 rounded-2xl bg-slate-950 border border-slate-800 min-h-48"><h2 className="font-bold">{steps[step]} information</h2><p className="text-sm text-slate-400 mt-2">Provide accurate information. Documents and location are used only for verification or active assignments under the platform’s privacy rules.</p></div><div className="flex justify-between mt-6"><button disabled={!step} onClick={()=>setStep(step-1)} className="px-4 py-2 text-sm text-slate-400">Back</button>{step<5?<button onClick={()=>setStep(step+1)} className="px-4 py-2 rounded-xl bg-indigo-600 text-sm">Continue</button>:<button onClick={submit} className="px-4 py-2 rounded-xl bg-indigo-600 text-sm">Submit application</button>}</div></div></div>}
